<div>
    <h3>
        {{post.user.username}}
    </h3>
    <h2>
        {{post.title}}
    </h2>
    <h3>
        {{post.body}}
    </h3>

    {{#if post.Comments}}
        {{#each post.Comments}}
            {{user.username}}
            {{body}}
            <br>
        {{/each}}
    {{/if}}

</div>